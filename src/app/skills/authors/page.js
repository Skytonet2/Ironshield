"use client";
// /skills/authors — author leaderboard page (Tier 5 slice 4).

import SkillsShell from "@/components/skills/SkillsShell";
import SkillsAuthorsPage from "@/components/skills/SkillsAuthorsPage";

export default function Page() {
  return (
    <SkillsShell>
      <SkillsAuthorsPage />
    </SkillsShell>
  );
}
